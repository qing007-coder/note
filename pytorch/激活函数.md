隐藏
    ReLU > Leaky ReLU > PReLU > Tanh > Sigmoid
输出层:
    二分类:Sigmoid
    多分类:Softmax
    回归问题:identity

二分类输出 → Sigmoid
多分类输出 → Softmax
不知道用啥隐藏层 → ReLU（最简单稳妥）
做 Transformer / 大模型 → GELU
RNN 系列 → Tanh
追求极致精度且算力够 → Swish / Mish

